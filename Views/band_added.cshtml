@{
	Layout="Shared/_Layout.cshtml";
}

<h1>You added @Model.Name band to this venue.</h1>
<hr />
@if (@Model.Getbands().Count != 0)
{
  <h4>Here are all the bands registered with this venue:</h4>
  <ul>
    @foreach (var band in @Model.Getbands())
    {
      <li><a href="/band/@band.Id">@band.Name</a></li>
    }
  </ul>
}
<!-- else
{
	<h4>This student is not enrolled in any bands.</h4>
}
@* <h4>Add a category to this task:</h4>
<form action='/task/add_category' method='post'>
  <input id='task-id' name='task-id' type='hidden' value='@Model["task"].GetId()'>
  <label for='category-id'>Select a category</label>
  <select id='category-id' name='category-id' type='text'>
    @foreach (var category in @Model["allCategories"])
    {
      <option value='@category.GetId()'>@category.GetName()</option>
    }
  </select>
  <button type='submit'>Add</button>
</form> *@ -->

<p><a href="/">Return to Main Page</a></p>
<p><a href="/venue/@Model.Id/bands/add">Click here to register bands to venue</a></p>
